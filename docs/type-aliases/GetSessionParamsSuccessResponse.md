[@grabjs/superapp-sdk](../globals.md) / GetSessionParamsSuccessResponse

# Type Alias: GetSessionParamsSuccessResponse

> **GetSessionParamsSuccessResponse** = [`SuccessResponse`](SuccessResponse.md)\<`string`\>

Success response for getSessionParams

## Remarks

The native layer returns session parameters as a JSON string. Parse with `JSON.parse(result)` to use as an object.
