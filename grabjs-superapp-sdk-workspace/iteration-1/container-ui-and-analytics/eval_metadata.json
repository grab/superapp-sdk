{
  "eval_id": 4,
  "eval_name": "container-ui-and-analytics",
  "prompt": "How do I hide the native back and refresh buttons, set a custom title bar color, and fire an analytics event when my MiniApp's checkout page loads?",
  "assertions": [
    "Uses ContainerModule methods for hiding the back button and refresh button",
    "Uses ContainerModule to set the title and/or background color",
    "Uses ContainerModule.sendAnalyticsEvent (or equivalent) to fire an analytics event"
  ]
}
