{
  "eval_id": 2,
  "eval_name": "checkout-trigger-payment",
  "prompt": "Walk me through triggering a payment when the user taps 'Pay Now' in my MiniApp, including how to tell if it succeeded or failed.",
  "assertions": [
    "Uses CheckoutModule (or its trigger-checkout method) to initiate the payment",
    "Uses isSuccess/isOk and isError type guards to branch on the response",
    "Does not wrap the SDK call in try/catch as the primary error-handling mechanism"
  ]
}
